# Local Claude Code settings — per-machine, never committed.
.claude/settings.local.json

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp

# node
node_modules/

# Playwright runtime artifacts (reports, screenshots from failed runs).
# Visual regression baselines under tests/visual/**/*.png are TRACKED via LFS.
test-results/
playwright-report/

