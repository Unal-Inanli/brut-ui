---
title: Combobox
description: Searchable single-select with a text input that filters a static option list and a hidden input mirror for form submission.
---

# Combobox

Searchable single-select with a text input that filters a static option list and a hidden input mirror for form submission.

<iframe
  class="brut-preview-frame"
  src="../preview/components-combobox.html"
  loading="lazy"
  style="height: 720px;"
  title="Combobox preview"
></iframe>

::: tip Markup source
The preview above is served from `preview/components-combobox.html` —
the canonical fixture for this component. Copy markup directly from
the source to use it in your own page.
:::

## Where to look

- **Source CSS:** [`src/components.css`](https://github.com/Unal-Inanli/brut-ui/blob/main/src/components.css) (search for `.brut-combobox`)
- **Manifest entry:** [`dist/components.json`](https://github.com/Unal-Inanli/brut-ui/blob/main/dist/components.json) (filter by `name`)
- **Reference:** [Manifest schema](/reference/manifest)
